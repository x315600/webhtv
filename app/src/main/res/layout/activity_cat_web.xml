<?xml version="1.0" encoding="utf-8"?>
<!--
    猫源设置中心的内嵌容器。

    与 activity_web_reader 的差别：那个加载本地模板、内容由 Java 注入；这个直接渲染猫源
    自己的远程页面，所以顶部保留一条地址栏 —— 页面来自本机 bundle，但用户得看得见自己在哪。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FF101216"
    android:orientation="vertical">

    <TextView
        android:id="@+id/address"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#FF1B1F26"
        android:ellipsize="middle"
        android:paddingHorizontal="16dp"
        android:paddingVertical="8dp"
        android:singleLine="true"
        android:textColor="#FF9AA2AF"
        android:textSize="12sp"
        tools:text="http://127.0.0.1:9988/website" />

    <ProgressBar
        android:id="@+id/progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:max="100"
        android:progress="0"
        android:visibility="gone" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <WebView
            android:id="@+id/web_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <LinearLayout
            android:id="@+id/loading"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#FF101216"
            android:gravity="center"
            android:orientation="vertical">

            <ProgressBar
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:indeterminate="true" />

            <TextView
                android:id="@+id/loading_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="20dp"
                android:gravity="center"
                android:paddingHorizontal="32dp"
                android:textColor="#FFE6E8EC"
                android:textSize="16sp"
                tools:text="正在打开设置中心" />

        </LinearLayout>

    </FrameLayout>

</LinearLayout>
